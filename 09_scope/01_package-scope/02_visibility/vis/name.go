package vis

var MyName = "Dromeo"               //MyName is capitalized so it is exported, visible outside of the package; can be called
var yourName = "Future Billionaire" //yourname is not capitalized so it is not exported, not visible outside of
//the package
