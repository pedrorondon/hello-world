package main

import "fmt"

func main() { // func keyword, receiver if any, function name, declare parameters/pass arguments
	// in parens (), returns, code in between curly braces {}
	fmt.Println("Hola Mundo!")
}

// main is the entry point to your program; you can only have one func main in entire program
// and needs to be inside package main; however, you can have packages w/o func main
